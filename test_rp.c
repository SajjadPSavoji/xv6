#include "types.h"
#include "stat.h"
#include "user.h"

int
main(int argc, char *argv[])
{
    // if( argc != 3)
    // {
    //     printf(2 , "rp gets 2 arguments !");
    //     exit();
    // }
    // int pid , rp;
    // pid = atoi(argv[1]);
    // rp = atoi(argv[2]);
    //change_rp(pid , rp);
    exit();
}
